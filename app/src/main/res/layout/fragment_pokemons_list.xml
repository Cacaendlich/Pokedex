<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".presenter.ui.pokemonsList.PokemonsListFragment">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/background_list"/>


    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_gravity="center"
        android:visibility="gone"
        android:progressTint="@color/white"
        android:indeterminateTint="@color/yellow"
        android:background="@drawable/pokeball_background"/>

    <TextView
        android:id="@+id/textViewEmptyList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/voc_ainda_n_o_favoritou_nenhum_pok_mon"
        android:textColor="@color/white"
        android:layout_gravity="center"
        android:visibility="gone"
        android:gravity="center"
        android:textSize="28sp"
        android:layout_margin="30dp"/>

    <ImageView
        android:id="@+id/iconEmptyList"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_gravity="center"
        android:visibility="gone"
        android:background="@drawable/empty"
        android:contentDescription="@string/ninho_vazio"
        android:layout_marginTop="100dp"/>

    <ProgressBar
        android:id="@+id/progressBarLoadMore"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_marginBottom="10sp"
        android:layout_gravity="center_horizontal|bottom"
        android:visibility="gone"
        android:progressTint="@color/white"
        android:indeterminateTint="@color/yellow"
        android:background="@drawable/pokeball_background"/>

</FrameLayout>